/**
 * Normalize simple tag typos used by the teleprompter (pure function).
 * @param {string} text
 * @returns {string}
 */
export declare function normalizeSimpleTagTypos(text: string): string;
export default normalizeSimpleTagTypos;
