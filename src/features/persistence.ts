// Persistence stub (localStorage wrappers etc.) - TS owned
export function initPersistence() {
  try { console.log('[src/features/persistence] initPersistence'); } catch {}
}

export const initPersistenceFeature = initPersistence;
